import LoginForm from "../components/LoginForm";

const LoginPage = () => {
    return (
        <div>
            <h1 className="text-center text-2xl font-bold">EmployWise Login</h1>
            <LoginForm />
        </div>
    );
};

export default LoginPage;
